# КРС · доска дизайн-задач

Страница генерируется из реестра сроков проекта скриптом `03_РАЗРАБОТКА/scripts/build-alena-board.mjs`
и публикуется сюда `publish-design-board.sh`. Руками `index.html` не править — перезапишется.

Публичная по ссылке, но закрыта от поисковиков: `noindex` в метатеге и `robots.txt`.
